{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copy non-ts files (chrome, unpacked)",
            "type": "shell",
            "command": "node build.js chrome chrome_build norm-nocompile",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Compile & Copy (chrome, unpacked)",
            "type": "shell",
            "command": "node build.js chrome chrome_build",
            "problemMatcher": [],
            "group": "none"
        }
    ]
}